import { combineReducers } from "redux";
import reducer from "./reducer";

export default combineReducers({
    reducer
})


//here export default because reducer imported not like {reducer}
//default and { } do not come together